/*The TransactionEnum.ts file does not contain any import or export statements, so it is not considered 
an ES Module by TypeScript The index.ts file does not contain any import or export statements, so it is
 not considered an ES Module by TypeScript
*/
enum TransactionEnum {
    success="success",
    failure="failure"
}
export {TransactionEnum}